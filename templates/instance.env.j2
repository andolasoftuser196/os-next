# Instance: {{ instance_name }} ({{ instance_type }})
# Subdomain: {{ instance_subdomain }}.{{ domain }}
# Generated: {{ generated_date }}
# Shared settings are in instances/shared.env

COMPOSE_PROJECT_NAME={{ instance_name }}

# Database (unique per instance)
DB_USERNAME={{ db_user }}
DB_PASSWORD={{ db_password }}
DB_NAME={{ db_name }}

# Security (unique per instance)
SECURITY_SALT={{ security_salt }}

# Redis prefix (unique per instance, prevents key collision)
{% if cache_engine == 'redis' %}
REDIS_PREFIX=cake_{{ instance_name }}_
{% endif %}
